<h1> Wiki Control Panel </h1>

<p>Search username or email for User</p>
<% form_tag wiki_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", (:name || :email) => nil %>
  </p>
<% end %> 